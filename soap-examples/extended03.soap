// contributions //
// last 2 years //
osmTag("highway", @typeOfStreet)
osmTag("maxspeed")
map(snapshot -> Geo.lengthOf(snapshot.getGeometry()))
x = sum()

x -= 2;
return x + 3;

lineageBy(e -> f(e, @hello_int))
//lineageBy(min)
